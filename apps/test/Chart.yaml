apiVersion: v2
name: {{ appName }}
description: 'Database deployment for {{ appName }}'
type: application
version: 1.0.0
appVersion: 1.0.0
dependencies:
  - name: database
    repository: "file://../db-chart"
    version: "0.1.0"
